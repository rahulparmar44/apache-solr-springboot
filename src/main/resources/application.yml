spring:
  datasource:
    url: jdbc:postgresql://localhost:2023/hashedindna
    username: postgres
    password: Password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: false
        format_sql: true